.DS_Store
*~
.#*

*.fasl
*.dx32fsl
*.dx64fsl
*.lx32fsl
*.lx64fsl
*.x86f

.serena/
.cache/
.direnv/
.mcp.json

# Test scratch space
tests/tmp/*
!tests/tmp/.gitkeep

# Dogfooding: throwaway scaffold projects and feedback log
experiments/
claudedocs/

# project-scaffold's default output directory.  Ignored in this repo only:
# scaffolds generated here are throwaway probes for exercising the tool, not
# deliverables.  Downstream projects should NOT copy this entry — there the
# generated skeleton is the artifact the user asked for.
scaffolds/

# AI agent working documents (designs, plans, reports)
docs/plans/
